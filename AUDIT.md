# 스킬 감사 기록

계정에 등록된 스킬 44개 전체를 감사한 결과다. 검사 방법과 판정 근거를 남겨,
다음에 같은 점검을 반복할 수 있게 한다.

## 대상 구분

| 구분 | 개수 | 이 저장소에서 수정 가능? |
|---|---|---|
| 이노쓰리디 자체 스킬 (`inno3d-*`, `dental-*`, `global-dental-*`) | 10 | 가능 — 이 저장소가 원본 |
| Anthropic 공식 (`docx`, `xlsx`, `pptx`, `pdf`) | 4 | 불가 — 상위에서 갱신됨 |
| Anthropic 예제 | 18 | 불가 — 상위에서 갱신됨 |
| 서드파티 (ui-ux-pro-max-cli 등) | 12 | 불가 — 패키지 갱신 시 덮어써짐 |

수정할 수 없는 34개는 고치지 않는다. 고쳐도 다음 갱신에 사라지고, 검증된 내용을
임의로 바꾸면 오히려 품질이 떨어진다. 대신 이노쓰리디 스킬이 그쪽 영역을 침범하지
않도록 경계를 맞추는 방식으로 대응한다.

## 34개 감사 결과 — 기계적 결함 없음

검사 항목: 프론트매터 YAML 유효성, `name`과 폴더명 일치, description 존재,
UTF-8 인코딩, 코드펜스 짝, 내부 파일 참조 유효성, 동봉된 Python 스크립트 문법.

결과: **치명·높음 등급 결함 0건.**

내부 참조 검사에서 10건이 걸렸으나 맥락을 확인한 결과 9건은 오탐이었다.
`assets/design-tokens.json` 같은 경로는 스킬이 읽는 파일이 아니라 사용자 프로젝트에
**만들어내는** 파일이며, 해당 SKILL.md가 "the scripts read and write project files"라고
명시하고 있다. `assets/banners/`도 "Output path convention"으로 표기돼 있다.

실제 결함으로 판단한 것은 1건이다.

| 스킬 | 내용 | 등급 |
|---|---|---|
| `design-system` | "Reference Implementation / Working example"로 제시된 `assets/designs/slides/claudekit-pitch-251223.html`가 패키지에 없음 | 낮음 — 예제를 찾다 실패할 뿐 동작에 지장 없음 |

서드파티 패키지라 수정하지 않는다. 갱신 시 덮어써지기 때문이다.

## 실행 환경 요구 스킬 — 대부분 스스로 물러난다

claude.ai 채팅에서 도구가 없을 때 어떻게 되는지 확인했다.

| 스킬 | 자체 대처 |
|---|---|
| `deep-research` | "서브에이전트 도구가 없으면 직접 조사하라"고 명시 — 정상 대체 |
| `computer-use` | 활성화 실패 시 "이유를 한 줄로 알리고 할 수 있는 일을 계속한다" |
| `built-in-browser` | "도달할 수 없을 때" 절을 따로 둠 |
| `chrome-browser` | 활성화 도구 선호출 경로 있음 |
| `morning` | 명시적 대처 문구 없음. 사용자가 브리핑을 직접 요청할 때만 발동 |
| `webapp-testing` | 명시적 대처 문구 없음. 로컬 웹앱 테스트를 요청할 때만 발동 |

모두 사용자가 해당 작업을 직접 요청할 때만 발동하므로, 치과 영업·마케팅 업무
중에 잘못 끼어들지 않는다. 남는 비용은 설명문이 차지하는 컨텍스트뿐이다.

## 이노쓰리디 스킬 10개 — 발견하고 고친 결함

본문 약 960줄을 전부 읽고 감사했다. 상세 내역은 커밋 이력을 참조한다.

- 설명문이 서로 같은 키워드를 주장해 자동 호출이 엉키던 문제 (마스터가 9개를 가림)
- 타 계정 스킬(`banner-design`, `brand`, `internal-comms` 등) 영역 침범
- `references/` 파일 8개가 어디서도 링크되지 않아 도달 불가능
- 존재하지 않는 `.docx` 템플릿을 있는 것처럼 참조
- 108일 지난 딜러 현황에 시점 경고 없음
- 지난 일정이 미완료 과제로 남음
- 회의록 구조가 교육 참조 파일에 잘못 들어감
- 사용자를 반말로 호칭 (다른 스킬의 자체 규칙과 모순)

## 검증 방법

```bash
python3 scripts/validate.py
```

`scripts/account-skills.txt`에 기록된 계정 스킬 목록을 기준으로, 위임 참조가
실재하는 스킬을 가리키는지까지 검사한다. 고의로 결함을 심어 검증기가 실제로
잡아내는지도 확인했다(이름 불일치, 반말 호칭, 참조 파일 삭제, 필수 섹션 제거,
마크다운 파손, 없는 스킬 지목 — 6종 모두 검출).
